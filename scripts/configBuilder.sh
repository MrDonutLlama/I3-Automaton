#Should be run automatically after bootstrapper.sh finishes its installations
#Builds (or edits) the user config files for i3, i3blocks, and other associated applications
#make read top down from each file, and programatically generate only what is needed in the files to minimize runtime and filesize
